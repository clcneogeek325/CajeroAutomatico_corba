module OperacionesCajero{
	interface Operaciones{
		boolean respuesta(in string usuario,in string contrasenia);
	
	        void retirar (in string id_usuario , in int  cantidad );
	        
	        void depositar(in string id_usuario , in int  cantidad );
	        
	        int  consultar(in string id_usuario );
	};
	
};
